# @react-playground/federation

This package provides various utility methods and reference types to be shared between the other modules in the application.

## Getting Started

- This package relies on the MonoRepo structure of the project, and running an `npm install` in the root is enough to build and start utilizing this module in any applications.
